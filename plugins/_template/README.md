# _template (not a published plugin)

Copy-paste starting point for a new mcp-stash solution. Never listed in
`.claude-plugin/marketplace.json` and never installable by clients.

Use `./scripts/new_plugin.sh <slug> "description"` from the repo root
instead of copying this by hand.
